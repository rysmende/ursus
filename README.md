# ursus.kg

A simple static website served at [ursus.kg](https://ursus.kg).

Hosted on Cloudflare Pages. Every push to `main` triggers an automatic deploy.

## Files
- `index.html` — the entire site (single static page)
